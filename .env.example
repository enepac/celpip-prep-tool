# CELPIP Speaking Prep — Environment Variables Template
#
# Copy this file to .env.local and fill in actual values.
# Never commit .env.local. The .gitignore enforces this.
#
# Variables get added to this template as features are built.
# Step 5 (Whisper) will add OPENAI_API_KEY.
# Step 7 (Scorer) will add ANTHROPIC_API_KEY.
# Step 9 (Deploy) may add additional Vercel-specific vars.

# (no required variables yet)
